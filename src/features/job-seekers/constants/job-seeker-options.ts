export const locationOptions = [
  "All Locations",
  "San Francisco, CA",
  "Remote (NYC)",
  "Seattle, WA",
] as const;

export const salaryOptions = ["Any Range", "$100k-$149k", "$150k+"] as const;
export const availabilityOptions = ["All Availability", "Active", "Open to Work", "Placed"] as const;
export const verificationOptions = ["Any Status", "Verified", "Unverified"] as const;
